# Requirements for Streamlit RAG Chatbot with Gemini API and ChromaDB

# Streamlit framework
streamlit

# Google Gemini API SDK (The recommended library)
google-genai

# Vector Store (ChromaDB) and its dependencies
# USE THIS LINE to install ChromaDB with all dependencies (includes pypdf, onnxruntime, etc.)
chromadb[all]

# The specific sentence-transformer model we use (also handles one of the deep dependencies)
sentence-transformers

# Components from the LangChain ecosystem
langchain-text-splitters
langchain-core
langsmith

# Document Loaders/Parsers
# NOTE: pypdf is now included in chromadb[all], but keeping it for safety.
pypdf 
requests
pandas
beautifulsoup4
lxml

# Dependency for Text-to-Speech (TTS) functionality
edge-tts

# CRITICAL FIX for asynchronous operation (TTS) within Streamlit
nest-asyncio
